# Add files here to ignore them from prettier formatting
/dist
/coverage
/.nx/cache
/.nx/workspace-data
/.nx/migrate-runs
.angular

# Full copies of the workspace, for the same reason vitest.config.mts skips it
.claude

# Generated by server/seed.mjs on every `npm run api`
/server/db.json

# Standalone reference documents, not source: reformatting them is 12k lines of
# churn on files nothing builds
/docs/*.html
