{
  "name": "appointments-shell",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "libs/appointments/shell/src",
  "prefix": "app",
  "tags": ["scope:appointments", "type:shell"],
  "targets": {
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "parallel": false,
        "commands": [
          "npx tsc -p libs/appointments/shell/tsconfig.lib.json --noEmit",
          "npx tsc -p libs/appointments/shell/tsconfig.spec.json --noEmit"
        ]
      }
    },
    "test": {
      "executor": "@nx/angular:unit-test",
      "options": {
        "buildTarget": "book:build:development",
        "tsConfig": "libs/appointments/shell/tsconfig.spec.json",
        "runner": "vitest",
        "include": ["libs/appointments/shell/src/**/*.spec.ts"],
        "watch": false
      },
      "configurations": {
        "watch": {
          "watch": true
        }
      }
    }
  }
}
