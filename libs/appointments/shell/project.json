{
  "name": "appointments-shell",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "libs/appointments/shell/src",
  "prefix": "app",
  "tags": ["scope:appointments", "type:shell"],
  "targets": {
    "lint": { "executor": "@nx/eslint:lint" },
    "test": {
      "executor": "nx:run-commands",
      "options": { "command": "npx vitest run libs/appointments/shell/src --passWithNoTests" }
    }
  }
}
