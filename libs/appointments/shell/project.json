{
  "name": "appointments-shell",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "libs/appointments/shell/src",
  "prefix": "app",
  "tags": ["scope:appointments", "type:shell"],
  "targets": {
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "parallel": false,
        "commands": [
          "npx tsc -p libs/appointments/shell/tsconfig.lib.json --noEmit",
          "npx tsc -p libs/appointments/shell/tsconfig.spec.json --noEmit"
        ]
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx vitest run libs/appointments/shell/src --passWithNoTests"
      }
    }
  }
}
