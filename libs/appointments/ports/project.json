{
  "name": "appointments-ports",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "libs/appointments/ports/src",
  "tags": ["scope:appointments", "type:ports"],
  "targets": {
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx tsc -p libs/appointments/ports/tsconfig.lib.json --noEmit"
      }
    }
  }
}
